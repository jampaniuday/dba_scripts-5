    rman nocatalog <<FIN_RMAN | grep Deleted 2>/dev/null
connect target /

#Deleting old backups and check ARC deleted from Recovery Area
run {
    DELETE FORCE NOPROMPT ARCHIVELOG UNTIL TIME 'SYSDATE-1';
    CROSSCHECK ARCHIVELOG ALL;
    DELETE FORCE NOPROMPT EXPIRED ARCHIVELOG ALL;
    DELETE FORCE NOPROMPT BACKUP COMPLETED BEFORE 'SYSDATE-15';
    CROSSCHECK BACKUP;
    DELETE FORCE NOPROMPT EXPIRED BACKUP;
}


SELECT NAME,
ROUND(SPACE_LIMIT / 1048576) SPACE_LIMIT_MB,
ROUND(SPACE_USED / 1048576) SPACE_USED_MB,
ROUND(((SPACE_USED / 1048576) * 100) / (SPACE_LIMIT / 1048576), 2) PRC_USED
FROM V$RECOVERY_FILE_DEST; 